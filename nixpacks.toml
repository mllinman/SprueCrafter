[phases.setup]
nixPkgs = ["python39", "gcc", "gxx", "stdenv", "libGL"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 120 --worker-class sync src.backend.app:app"
