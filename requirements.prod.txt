# Production-specific Python dependencies (optional)
# These are only needed if deploying as a hosted service

# Production server
gunicorn==23.0.0

# Environment configuration
python-dotenv==1.0.1

# Payment processing (for Pro subscriptions)
stripe==11.3.0

